# Logging
color_log               : False  # Colors the console output. May not work on some systems
verbosity               : DEBUG  # Verbosity of log messages in the console.
                                 # These are the valid levels: DEBUG, INFO, WARNING, ERROR, CRITICAL
                                 # Full log will always be available in a file.

# Debug
#saveslot                : 3      # Set to 0 or remove to start new game
#checkpoint              : "edel_expr" # Testing, remove to play from start
